# Design rules for Defaults

This document describes things to keep in mind when creating defaults


## What should go in defaults?

Defaults is not a place for engine code.
**We should be able to compile the engine, even when the Defaults folder has been deleted!**


